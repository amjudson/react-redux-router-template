import * as constants from './serverConstants';
import colors from 'colors';

/* eslint-disable no-console */
console.log('Starting app in dev mode...'.blue);
console.log(`Loading on http://localhost:${constants.port}`.green.underline);
